# A basic setup for front-end project
* Set up in a TDD fashion
* Stack: Gulp, npm, Browserify, Karma, Mocha and Chai

## Usage
* ```npm install``` 
* ```gulp install```
* ```gulp test```  
* ```gulp tdd``` 
* ```gulp bundle``` 